// Backend mirror intentionally left minimal.
// Frontend reconcile components live under src/components/dashboard/reconcile.
// This placeholder exists so TypeScript does not attempt to parse JSON
// artifacts that were mistakenly committed in this path.

export {};
